class TurretInfo {

    name: string
    description: string
    range: number
    dps: string

    constructor(name: string, description: string, range: number, dps: string) {
        this.name = name
        this.description = description
        this.range = range
        this.dps = dps
    }
}
